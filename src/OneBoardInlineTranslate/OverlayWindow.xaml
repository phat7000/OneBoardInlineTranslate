<Window x:Class="OneBoardInlineTranslate.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OneBoard Inline Translate"
        Width="460"
        MinHeight="150"
        MaxHeight="360"
        SizeToContent="Height"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        ShowActivated="False"
        ShowInTaskbar="False"
        Focusable="False"
        IsHitTestVisible="False"
        Topmost="True">
  <Border Margin="8"
          Padding="16"
          CornerRadius="12"
          Background="#F21B1D24"
          BorderBrush="#54657A"
          BorderThickness="1">
    <Border.Effect>
      <DropShadowEffect BlurRadius="18" ShadowDepth="3" Opacity="0.42" />
    </Border.Effect>
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>

      <DockPanel Grid.Row="0" LastChildFill="True">
        <TextBlock x:Name="StatusText"
                   Foreground="#84D8FF"
                   FontSize="13"
                   FontWeight="SemiBold"
                   Text="CAPTURED" />
        <TextBlock Text="ONEBOARD · PHASE 0"
                   DockPanel.Dock="Right"
                   HorizontalAlignment="Right"
                   Foreground="#8F98A8"
                   FontSize="10"
                   FontWeight="SemiBold" />
      </DockPanel>

      <TextBlock x:Name="ProcessText"
                 Grid.Row="1"
                 Margin="0,8,0,0"
                 Foreground="#B7C0CD"
                 FontFamily="Consolas"
                 FontSize="12" />

      <Border Grid.Row="2"
              Margin="0,10,0,10"
              Padding="10"
              MaxHeight="220"
              Background="#A6111217"
              CornerRadius="6">
        <ScrollViewer VerticalScrollBarVisibility="Hidden"
                      HorizontalScrollBarVisibility="Disabled"
                      CanContentScroll="False">
          <TextBlock x:Name="CapturedText"
                     Foreground="#F3F5F7"
                     FontSize="14"
                     TextWrapping="Wrap" />
        </ScrollViewer>
      </Border>

      <Grid Grid.Row="3">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="MethodText"
                   Foreground="#AEB7C4"
                   FontSize="11" />
        <TextBlock x:Name="TimingText"
                   Grid.Column="1"
                   Foreground="#AEB7C4"
                   FontFamily="Consolas"
                   FontSize="11" />
      </Grid>

      <TextBlock Grid.Row="4"
                 Margin="0,8,0,0"
                 Foreground="#687383"
                 FontSize="10"
                 Text="Alt+Q capture  ·  Alt+E test replacement" />
    </Grid>
  </Border>
</Window>
